user = _DB_USER
password = _DB_PWD
hosts = _DB_HOST
dbname = _DB_NAME
#optional query to use when relaying for backup MX
#query = SELECT domain FROM domain WHERE domain = '%d' AND active = true AND backupmx = false
query = SELECT domain FROM domain WHERE domain = '%d' AND active = true
