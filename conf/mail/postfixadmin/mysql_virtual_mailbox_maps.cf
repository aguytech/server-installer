user = _DB_USER
password = _DB_PWD
hosts = _DB_HOST
dbname = _DB_NAME
#query = SELECT maildir FROM mailbox WHERE username='%s' AND active = 1 UNION DISTINCT SELECT maildir FROM mailbox, alias_domain WHERE alias_domain.alias_domain = '%d'  AND mailbox.active = 1 AND alias_domain.active = 1 AND mailbox.username = CONCAT ( '%u', '@', alias_domain.target_domain )
query = SELECT maildir FROM mailbox WHERE username='%s' AND active = 1
